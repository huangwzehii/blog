# Railway 部署完整指南

## 📦 方案选择

### 方案A: 使用 GitHub（推荐）⭐⭐⭐⭐⭐
- ✅ 自动部署
- ✅ 代码版本管理
- ✅ 支持持续集成
- ⚠️ 需要安装 Git

### 方案B: 直接从本地部署
- ✅ 不需要 Git
- ✅ 快速上传
- ⚠️ 需要安装 Railway CLI

---

## 🎯 方案A: GitHub 部署（推荐）

### 步骤1: 安装 Git

**Windows:**
1. 访问 https://git-scm.com/download/win
2. 下载并安装 Git
3. 安装时全部选择默认选项
4. 安装完成后重启命令行

**验证安装:**
```bash
git --version
```

### 步骤2: 创建 GitHub 账号

1. 访问 https://github.com
2. 点击 "Sign up"
3. 按提示创建账号

### 步骤3: 创建 GitHub 仓库

1. 登录 GitHub
2. 点击右上角 "+" → "New repository"
3. 填写信息：
   - Repository name: `blog`（或其他名字）
   - Description: `双人博客系统`
   - 选择 "Public" 或 "Private"
   - **不要**勾选 "Add a README file"
4. 点击 "Create repository"

### 步骤4: 上传代码到 GitHub

在你的项目目录（D:\blog）打开命令行，执行：

```bash
# 初始化 Git 仓库
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit"

# 添加远程仓库（替换成你的仓库地址）
git remote add origin https://github.com/你的用户名/blog.git

# 推送代码
git branch -M main
git push -u origin main
```

**如果提示需要登录：**
- 输入你的 GitHub 用户名
- 密码使用 Personal Access Token（不是账号密码）

**创建 Personal Access Token:**
1. GitHub → Settings → Developer settings
2. Personal access tokens → Tokens (classic)
3. Generate new token
4. 勾选 "repo" 权限
5. 生成并复制 token
6. 在命令行中粘贴这个 token 作为密码

### 步骤5: 部署到 Railway

1. **访问** https://railway.app
2. **点击** "Login" → "Login with GitHub"
3. **授权** Railway 访问你的 GitHub
4. **点击** "New Project"
5. **选择** "Deploy from GitHub repo"
6. **选择** 你刚创建的 `blog` 仓库
7. **等待** 部署完成（1-2分钟）

### 步骤6: 配置域名

部署完成后：
1. 点击你的项目
2. 点击 "Settings" 标签
3. 找到 "Domains" 部分
4. 点击 "Generate Domain"
5. 你会得到一个域名，如：`blog-production-xxxx.up.railway.app`

### 步骤7: 测试

1. 访问你的 Railway 域名
2. 应该能看到你的博客首页
3. 登录并测试发布功能

---

## 🎯 方案B: Railway CLI 部署

### 步骤1: 安装 Railway CLI

**Windows (使用 npm):**
```bash
npm install -g @railway/cli
```

**验证安装:**
```bash
railway --version
```

### 步骤2: 登录 Railway

```bash
railway login
```

这会打开浏览器，登录你的 Railway 账号。

### 步骤3: 初始化项目

在项目目录（D:\blog）执行：

```bash
# 初始化 Railway 项目
railway init

# 选择 "Create a new project"
# 输入项目名称，如 "blog"
```

### 步骤4: 部署

```bash
railway up
```

等待上传和部署完成。

### 步骤5: 获取域名

```bash
railway domain
```

按提示生成域名。

---

## 📝 部署后配置

### 查看日志

**方法1: Railway 网页**
1. 进入你的项目
2. 点击 "Deployments"
3. 点击最新的部署
4. 查看 "Deploy Logs"

**方法2: CLI**
```bash
railway logs
```

### 查看环境变量

Railway 会自动设置 `PORT` 环境变量，你的代码已经支持：
```javascript
const PORT = process.env.PORT || 3000;
```

### 重新部署

**如果使用 GitHub:**
```bash
git add .
git commit -m "更新代码"
git push
```
Railway 会自动检测并重新部署。

**如果使用 CLI:**
```bash
railway up
```

---

## 🔧 常见问题

### 问题1: 部署失败

**查看日志:**
```bash
railway logs
```

**常见原因:**
- 依赖安装失败 → 检查 package.json
- 端口配置错误 → 确保使用 process.env.PORT
- 启动命令错误 → 检查 package.json 的 start 脚本

### 问题2: 数据丢失

Railway 的文件系统是临时的，重启后会丢失。

**解决方案:**
1. 使用 Railway 的 PostgreSQL 插件（推荐）
2. 定期备份 SQLite 数据库
3. 使用外部数据库服务

**添加 PostgreSQL:**
1. 在 Railway 项目中点击 "New"
2. 选择 "Database" → "PostgreSQL"
3. 等待创建完成
4. Railway 会自动设置环境变量

### 问题3: 访问速度慢

Railway 服务器在国外，国内访问可能较慢。

**解决方案:**
- 使用 CDN
- 或考虑使用国内服务器

---

## 📊 Railway 免费额度

- ✅ 每月 500 小时运行时间
- ✅ 512 MB 内存
- ✅ 1 GB 磁盘空间
- ✅ 无限带宽

**足够个人博客使用！**

---

## 🎯 推荐配置

### package.json 检查

确保有正确的 start 脚本：
```json
{
  "scripts": {
    "start": "node server.js"
  }
}
```

### .gitignore 检查

确保不上传这些文件：
```
node_modules/
.env
blog.db
uploads/
```

---

## 🚀 快速命令参考

```bash
# Git 相关
git status              # 查看状态
git add .               # 添加所有文件
git commit -m "消息"    # 提交
git push                # 推送到 GitHub

# Railway CLI 相关
railway login           # 登录
railway init            # 初始化项目
railway up              # 部署
railway logs            # 查看日志
railway open            # 在浏览器中打开项目
railway domain          # 管理域名
```

---

## 📞 需要帮助？

如果遇到问题：

1. **查看日志**
   ```bash
   railway logs
   ```

2. **检查部署状态**
   - 访问 Railway 控制台
   - 查看 Deployments 标签

3. **常见错误**
   - 端口错误 → 确保使用 process.env.PORT
   - 依赖错误 → 运行 npm install 检查
   - 启动错误 → 检查 package.json

---

## ✅ 部署成功标志

部署成功后，你应该能：
- ✅ 访问 Railway 域名看到网站
- ✅ 登录账号
- ✅ 发布碎碎念
- ✅ 发布文章
- ✅ 上传相册
- ✅ 发表评论

---

**祝部署顺利！🎉**
