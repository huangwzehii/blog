# 锁死 - 双人博客系统

一个简洁优雅的双人博客系统，支持碎碎念、文章和相册功能。

## 功能特点

### 📝 内容管理
- **碎碎念** - 快速记录日常想法
- **文章** - 发布长篇内容
- **相册** - 上传和分享照片

### 👥 双账号系统
- 男性账号和女性账号
- 独立的内容发布和管理
- 互相评论和互动

### 🎨 界面设计
- 温馨的金色主题
- 响应式设计，支持移动端
- 流畅的动画效果
- 优雅的照片查看器

### 📅 特色功能
- **天数计时器** - 记录相识天数
- **内容日历** - 可视化查看发布历史
- **通知系统** - 实时提醒新内容
- **搜索功能** - 快速查找内容
- **分页浏览** - 优化加载性能

## 技术栈

- **前端**: HTML5, CSS3, JavaScript (原生)
- **后端**: Node.js, Express
- **数据库**: SQLite3
- **文件上传**: Multer

## 快速开始

### 环境要求
- Node.js >= 14.0.0
- npm >= 6.0.0

### 安装步骤

1. **克隆项目**
```bash
git clone <repository-url>
cd blog
```

2. **安装依赖**
```bash
npm install
```

3. **启动服务**
```bash
node server.js
```

4. **访问应用**
打开浏览器访问: http://localhost:3000

### 默认账号

- **男性账号密码**: `sr`
- **女性账号密码**: `nxz`

⚠️ **重要**: 部署到生产环境前请修改密码！

## 项目结构

```
blog/
├── public/              # 前端静态文件
│   ├── index.html      # 主页面
│   ├── script.js       # 前端逻辑
│   └── style.css       # 样式文件
├── uploads/            # 照片存储目录
├── server.js           # 服务器主文件
├── blog.db             # SQLite数据库
├── package.json        # 项目配置
└── README.md           # 项目说明
```

## 配置说明

### 修改端口
编辑 `server.js`:
```javascript
const PORT = process.env.PORT || 3000;
```

### 修改密码
编辑 `public/script.js`:
```javascript
const PASSWORDS = {
    male: 'your-password',
    female: 'your-password'
};
```

### 修改计时器起始日期
编辑 `public/script.js`:
```javascript
const startDate = new Date('2025-12-01T00:00:00+08:00');
```

## 部署指南

详细的部署说明请查看 [DEPLOY.md](DEPLOY.md)

### 推荐部署方式

使用 PM2 管理进程:
```bash
npm install -g pm2
pm2 start server.js --name blog
pm2 save
pm2 startup
```

### Nginx 反向代理
```nginx
server {
    listen 80;
    server_name yourdomain.com;
    
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

## 数据备份

### 备份数据库
```bash
cp blog.db blog.db.backup
```

### 备份照片
```bash
tar -czf uploads-backup.tar.gz uploads/
```

## API 接口

### 内容相关
- `GET /api/posts/:type` - 获取内容列表
- `POST /api/posts` - 创建内容
- `PUT /api/posts/:id` - 更新内容
- `DELETE /api/posts/:id` - 删除内容

### 评论相关
- `POST /api/comments` - 发表评论

### 相册相关
- `GET /api/albums` - 获取相册列表
- `POST /api/albums` - 上传相册
- `DELETE /api/albums/:id` - 删除相册

## 浏览器支持

- Chrome (推荐)
- Firefox
- Safari
- Edge
- 移动端浏览器

## 安全建议

1. ✅ 修改默认密码
2. ✅ 使用 HTTPS
3. ✅ 定期备份数据
4. ✅ 限制上传文件大小
5. ✅ 更新依赖包

## 常见问题

### 端口被占用
```bash
# Windows
netstat -ano | findstr :3000
taskkill /PID <进程ID> /F

# Linux/Mac
lsof -i :3000
kill -9 <进程ID>
```

### 照片上传失败
检查 `uploads/` 目录权限:
```bash
chmod 755 uploads/
```

### 数据库错误
删除 `blog.db` 文件，重启服务会自动创建新数据库。

## 开发计划

- [ ] 支持Markdown编辑器
- [ ] 添加标签系统
- [ ] 支持内容导出
- [ ] 添加主题切换
- [ ] 支持多语言

## 更新日志

### v1.0.0 (2024-12-07)
- ✨ 初始版本发布
- ✨ 支持碎碎念、文章、相册
- ✨ 双账号系统
- ✨ 评论功能
- ✨ 天数计时器
- ✨ 内容日历
- ✨ 通知系统
- ✨ 搜索功能
- ✨ 响应式设计

## 许可证

MIT License

## 联系方式

如有问题或建议，欢迎提交 Issue。

---

Made with ❤️ for couples
